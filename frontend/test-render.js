const { createElement } = require('react');
const { renderToString } = require('react-dom/server');
const { Sidebar } = require('./src/components/Sidebar.tsx'); // We need Next.js environment, this might be hard.
